## Packages
framer-motion | For complex scroll-triggered animations and smooth reveals
react-scroll | For smooth scrolling navigation between sections
clsx | Utility for constructing className strings conditionally
tailwind-merge | Utility for merging Tailwind classes efficiently

## Notes
Tailwind Config - extend fontFamily:
fontFamily: {
  display: ["'Outfit', sans-serif"],
  body: ["'Inter', sans-serif"],
}
API uses /api/contact for form submission
Dynamic images for projects should be handled via valid URLs or placeholders if not available
